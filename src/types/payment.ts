export interface payment {
  id: string;
  salary: number;
  amount: number;
  date: Date;
  createdDate: Date;
  updatedDate: Date;
}

// ! change payment interface placement
// ! Employees & Salaries
